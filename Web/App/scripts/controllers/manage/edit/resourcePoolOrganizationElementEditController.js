//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Manual changes to this file may cause unexpected behavior in your application.
//     Manual changes to this file will be overwritten if the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

(function () {
    'use strict';

    var controllerId = 'resourcePoolOrganizationElementEditController';
    angular.module('main')
        .controller(controllerId, ['resourcePoolOrganizationElementService',
            'elementService',
            'resourcePoolService',
            'logger',
            '$location',
            '$routeParams',
            resourcePoolOrganizationElementEditController]);

    function resourcePoolOrganizationElementEditController(resourcePoolOrganizationElementService,
		elementService,
		resourcePoolService,
		logger,
		$location,
		$routeParams) {
        logger = logger.forSource(controllerId);

        var isNew = $location.path() === '/manage/resourcePoolOrganizationElement/new';
        var isSaving = false;

        // Controller methods (alphabetically)
        var vm = this;
        vm.elementSet = [];
        vm.resourcePoolSet = [];
        vm.cancelChanges = cancelChanges;
        vm.isSaveDisabled = isSaveDisabled;
        vm.resourcePoolOrganizationElement = null;
        vm.saveChanges = saveChanges;
        vm.hasChanges = hasChanges;

        initialize();

        /*** Implementations ***/

        function cancelChanges() {

            $location.path('/manage/resourcePoolOrganizationElement');

            if (resourcePoolOrganizationElementService.hasChanges()) {
                resourcePoolOrganizationElementService.rejectChanges();
                logWarning('Discarded pending change(s)', null, true);
            }
        }

        function hasChanges() {
            return resourcePoolOrganizationElementService.hasChanges();
        }

        function initialize() {

            elementService.getElementSet(false)
                .then(function (data) {
                    vm.elementSet = data;
                });

            resourcePoolService.getResourcePoolSet(false)
                .then(function (data) {
                    vm.resourcePoolSet = data;
                });

            if (isNew) {
                // TODO For development enviroment, create test entity?
            }
            else {
                resourcePoolOrganizationElementService.getResourcePoolOrganizationElement($routeParams.Id)
                    .then(function (data) {
                        vm.resourcePoolOrganizationElement = data;
                    })
                    .catch(function (error) {
                        // TODO User-friendly message?
                    });
            }
        };

        function isSaveDisabled() {
            return isSaving ||
                (!isNew && !resourcePoolOrganizationElementService.hasChanges());
        }

        function saveChanges() {

            if (isNew) {
                resourcePoolOrganizationElementService.createResourcePoolOrganizationElement(vm.resourcePoolOrganizationElement);
            } else {
                // To be able to do concurrency check, RowVersion field needs to be send to server
				// Since breeze only sends the modified fields, a fake modification had to be applied to RowVersion field
                var rowVersion = vm.resourcePoolOrganizationElement.RowVersion;
                vm.resourcePoolOrganizationElement.RowVersion = '';
                vm.resourcePoolOrganizationElement.RowVersion = rowVersion;
            }

            isSaving = true;
            resourcePoolOrganizationElementService.saveChanges()
                .then(function (result) {
                    $location.path('/manage/resourcePoolOrganizationElement');
                })
                .catch(function (error) {
                    // Conflict (Concurrency exception)
                    if (error.status === '409') {
                        // TODO Try to recover!
                    }
                })
                .finally(function () {
                    isSaving = false;
                });
        }
    };
})();
