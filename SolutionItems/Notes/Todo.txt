. remove obsolete github repo later on
. edmx + database - try to use cascade delete option?
. easy deployment;
.. web deployment?
.. sql local db or compact etc.?
.. sql migrations
. random data generation?
. users should only access to their own items?
. remove satisfaction word..?
. multiple pk for user organization + user sector + user license + user resource pool organization + resource pool organization
. ef tools?!
http://msdn.microsoft.com/en-us/data/jj593170.aspx
http://msdn.microsoft.com/en-US/data/jj200620
http://msdn.microsoft.com/en-US/data/jj591583#Index

.. use scripts.render?

. for license;
difference between current odatacontroller vs. original generated -> createdon + modifiedon

. breeze.debug.js modification
. createdon = datetime.specifykind(utc);
. edmbuilder modification

. try to merge list + edit.htmls ?!
how to handle route.js then?!
New comment: this may not be the correct way of doing it

. angularjs wire up a backend sample has a solution for multiple with with same context!

. merge getLicense + getLicenseSet

. vm - alphabetically

manager.fetchEntityByKey('Employee', employeeID)
       .then(function(data) {
            employee(data.entity); // KO binding updates the screen
       });